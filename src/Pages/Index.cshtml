@page
@model IndexModel

<!DOCTYPE html>
    <head>
        <title>KSUCS K-12 Outreach Map Tool</title>
        <link type="text/css" rel="stylesheet" href="css/site.css">
    </head>
    <body>

        <h1>K-State Computer Science K-12 Outreach Map Tool</h1>
        <p>
            This service provides a configurable map of Kansas K-12 
            school districts as an SVG file, accessible at <a href="@Model.BaseUrl/Map">@Model.BaseUrl/Map</a>
        </p> 
        <img src="@Model.BaseUrl/Map" alt="District Map"/>

        <h2>Marking School Districts</h2>
        <p>
            You can indicate specific school districts to highlight 
            by including a <code>districts</code> query parameter 
            containing a comma-deliniated list of school districts. 
            I.e. to highlight districts 368 and 407, you would use 
            the url:
        </p>
        <div>
            <code>@Model.BaseUrl/Map?districts=368,407</code>
        </div>
        <img src="@Model.BaseUrl/Map?districts=368,407" alt="Highight Example Map"/>

        <p>
            You can also supply a custom color to use for filling the 
            highlighted districts (the default is K-State purple). This 
            should be provided as the <code>fill</code> query parameter. 
            It can use HTML color names or hex color values (though you must 
            omit the "#"):
        </p>
        <div>
            <code>@Model.BaseUrl/Map?districts=312,403&fill=yellow</code>
        </div>
        <img src="@Model.BaseUrl/Map?districts=312,403&fill=yellow" alt="HTML Color Fill Example"/>
        
        <div>
            <code>@Model.BaseUrl/Map?districts=383&fill=44af</code>
        </div>
        <img src="@Model.BaseUrl/Map?districts=383&fill=44af" alt="Hex Fill Example"/>

        <h2>Modifying the Image</h2>
        <p>
            You can provide a custom size (default is 700x400 pixels) by supplying a width and/or height parameter
            in the query string.  For example, the following
            sets the map to be 200 pixels wide:
        </p>
        <div>
            <code>@Model.BaseUrl/Map?width=200</code>
        </div>
        <img src="@Model.BaseUrl/Map?width=200" alt="Custom Width Example"/>

        <p>
            When supplying both a width and height, map will be centered in the available space, 
            preserving the aspect ratio. 
        </p>
        <div>
            <code>@Model.BaseUrl/Map?width=400&height=400</code>
        </div>
        <img src="@Model.BaseUrl/Map?width=400&height=400" alt="Custom Width and Height Example"/>


    </body>
</html>